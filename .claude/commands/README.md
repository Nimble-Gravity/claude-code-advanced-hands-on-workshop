# Commands

Custom slash commands you build in the labs live here as version-controlled
prompts: `plan-feature.md` (Module 2) and `run-pipeline.md` (Module 5). They
should target the artifact schemas in `templates/`.
