# Module 3 scenario: Explore before you touch analytics

**Ticket (as received from the business):**

> We want to change how the activity summary groups ledger entries into
> regions (compliance wants finer buckets). Before anyone writes code, map
> what a change to region bucketing would touch.

**Where it lives:** `src/contoso/analytics.py`, `src/contoso/reporting.py`,
`src/contoso/posting.py`, `src/contoso/ledger.py`.

**Your job in this module is exploration + critique only — no code.** A canned
plan is provided at `reference/m3/plan.md`; copy it to `artifacts/m3/plan.md`
as the plan under review.

Use this for Module 3.
