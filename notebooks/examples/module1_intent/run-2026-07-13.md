# Module 1 — example successful execution log

Run date: 2026-07-13
Notebook: `notebooks/day1/module1_intent.ipynb`
Persona: workshop attendee, medium Claude Code experience.

This log demonstrates a completed run of the module end-to-end — cell
outputs and the self-check results — **without publishing the actual
intent.md / plan.*.md content**, since that's the exercise's solution.
Full artifacts (not tracked here) live in `artifacts/m1/` for this run.

## Cell-by-cell execution

### Setup check
```
project root: /Users/kevinbooth/anthropic/claude_code_advanced_workshop/claude-code-workshop
contoso imported OK
```

### Config constants
```
TRANSFER_LIMIT        = 100
THROTTLE_PER_SECOND    = 20
DEFAULT_WINDOW_SECONDS = 60
```

### Exercise (steps 1-3)
1. Read `scenarios/m1-transfer-limit.md`.
2. Hand-wrote `artifacts/m1/intent.md` from `templates/intent.md`.
3. Ran plan mode once per model against the identical intent doc, fresh
   session each time, saved to:
   - `artifacts/m1/plan.opus.md`
   - `artifacts/m1/plan.sonnet.md`
   - `artifacts/m1/plan.haiku.md`

### Artifact metadata (proof of completion, no content)

| file | words | lines | sha256 |
|---|---|---|---|
| intent.md | 567 | 58 | `40039e5cdd97d966...` |
| plan.opus.md | 1819 | 209 | `047c81f4f968819c...` |
| plan.sonnet.md | 1904 | 233 | `dca1c221111db0a3...` |
| plan.haiku.md | 1106 | 80 | `237df3ef3194c424...` |

### Self-check cell output
```
[x] intent.md present (567 words)
  [x] has an explicit 'does NOT' scope section?
  [x] pinned the TRANSFER_LIMIT unit?
  [x] states acceptance criteria?

Comparing 3 plans: opus, sonnet, haiku
```

### Gap matrix (heuristic keyword scan of each plan's "Explicit assumptions")
```
gap          |   opus   |  sonnet  |  haiku
---------------------------------------------
unit         |    x     |    x     |    x
currency     |    x     |    x     |    .       <- split
cross-border |    x     |    .     |    .       <- split
holds/posted |    x     |    x     |    x
scope        |    x     |    x     |    x
window       |    x     |    x     |    x
weekend      |    .     |    .     |    .

gaps addressed: opus 6/7, sonnet 5/7, haiku 4/7
```

## Result

Intent doc passed all three structural checks (does-NOT section, pinned
unit, acceptance criteria present). Six of seven candidate gaps were pinned
explicitly in the intent doc and held across all three models. Two gaps
(`currency`, `cross-border`) and one un-pinned gap (`weekend`) still show
model-tracked variance — the expected outcome of the exercise, since a
perfect score would mean nothing was left to observe.

## Notes / anomalies

- The Opus and Sonnet planning subagents each hit an API session-limit
  error mid-run but had already written their output files before dying —
  plans were still complete and usable. Worth flagging to facilitators:
  a real attendee running this across separate Claude Code sessions
  wouldn't have this same recovery.
- The heatmap/HTML visualization cell (`dd0f864f`) was not executed in this
  run — no Jupyter/IPython display surface was available in this
  environment. Its logic is a superset of the text self-check above and
  was validated by inspection of the notebook source.
- The optional `plan.opus.2.md` sampling-axis run was not produced in this
  pass.
